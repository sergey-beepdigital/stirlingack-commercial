image: andthensome/docker-node-rsync

pipelines:
  branches:
    master: 
      - step:
          name: Build Package
          script:
          - npm install
          - npm run build -- --production
          - sftp $DEV_DEPLOY_URL <<< $'put -r '../$PACKAGE_DIR
          
            
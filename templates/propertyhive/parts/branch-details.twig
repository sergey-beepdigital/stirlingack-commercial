{% if branch_data %}
    <div class="property-branch-details">
        <div>
            <a href="#"><i class="fa-light fa-phone"></i> {{ branch_data.phone }}</a>
            <span class="property-branch-details--rate">Standard rate</span>
        </div>
        <div>
            <a href="javascript:;" data-action="branchContact" data-department="{{ property._department == 'residential-sales' ? 'sales' : 'lettings' }}" data-id="{{ property.id }}"><i class="fa-light fa-envelope"></i> Contact Branch</a>
        </div>
    </div>
{% endif %}
